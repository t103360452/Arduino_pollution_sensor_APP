<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pollute</item>
        <item name="colorPrimaryDark">@color/pollute</item>
        <item name="android:textColorPrimary">@color/pollute</item>
        <item name="android:colorForeground">@color/pollute</item>
        <item name="android:colorBackground">@android:color/background_light</item>
    </style>

</resources>
